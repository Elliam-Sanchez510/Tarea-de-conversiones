// grados celsius a Fahrenheits //

let celsius = 50
let fahrenheit = (celsius* 1.8) + 32
console.log(` El resultado de ${celsius} grados celsius es: ${fahrenheit} grados fahrenheit.`);


// Grados Fahreinheits a celsius //

let  fahren = 122
let cels = (fahren - 32) * 5/9
console.log(` El resultado de ${fahren} grados Farenheits es: ${cels} grados celsius.`);